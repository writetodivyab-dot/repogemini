pipeline {
    // Using the most basic agent definition that should work
    agent any

    stages {
        stage('Minimal Test Stage') {
            steps {
                sh 'echo "Hello from the Test Stage!"'
                sh 'echo "If you see this message, the Jenkins environment is working."'
            }
        }
    }
}